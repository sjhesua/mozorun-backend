{
  "collectionName": "components_variants_variants",
  "info": {
    "name": "variants",
    "icon": "angle-double-right",
    "description": ""
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string"
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "SINGLE_SELECT",
        "MULTI_SELECT"
      ],
      "required": true
    },
    "types": {
      "type": "component",
      "repeatable": true,
      "component": "variants.types"
    },
    "SELECT_EXACTLY": {
      "type": "biginteger",
      "default": "0"
    }
  }
}
